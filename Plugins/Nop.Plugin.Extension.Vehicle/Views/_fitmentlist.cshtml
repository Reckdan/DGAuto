@model IEnumerable<Nop.Plugin.Extension.Vehicle.Domain.Fitment>
@using System
@using Nop.Web.FrameWork
@using Nop.Web.FrameWork.UI

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Make)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Engine)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Make)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Engine)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.VehicleID }) |
                @Html.ActionLink("Details", "Details", new { id = item.VehicleID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.VehicleID })*@
            </td>
        </tr>
    }

</table>